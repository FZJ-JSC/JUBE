#!/bin/bash -x
#MSUB -S /bin/bash
#MSUB -N #BENCHNAME#
#MSUB -M #NOTIFY_EMAIL#
#MSUB -m #NOTIFY_MODE#
#MSUB -l nodes=#NODES#:ppn=#NCPUS#
#MSUB -v tpt=#NTHREADS#
#MSUB -l walltime=#TIME_LIMIT#
#MSUB -o #STDOUTLOGFILE#
#MSUB -e #STDERRLOGFILE#

export OMP_NUM_THREADS=#NTHREADS#

cd $PBS_O_WORKDIR

#ENV#

#PREPROCESS#

#MEASUREMENT# #STARTER# #ARGS_STARTER# #EXECUTABLE# #ARGS_EXECUTABLE#

#POSTPROCESS#

touch #DONE_FILE#


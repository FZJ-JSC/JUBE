<?xml version="1.0" encoding="UTF-8"?>
<jube>
  <benchmark name="step_prepare_1" outpath="bench_run">
    <comment>step_prepare_1</comment>
     
    <parameterset name="options">
      <parameter name="iterations">1</parameter>
    </parameterset>

    <step name="perform_iterations_1" prepare="perform_iterations_2,perform_iterations_3">
      <use>options</use>
      <do>echo ${iterations}</do>
    </step>

    <step name="perform_iterations_2" prepare="perform_iterations_3">
      <use>options</use>
      <do>echo ${iterations}</do>
    </step>

    <step name="perform_iterations_3">
      <use>options</use>
      <do>echo ${iterations}</do>
    </step>

    <step name="perform_iterations_4" prepare="perform_iterations_5">
      <use>options</use>
      <do>echo ${iterations}</do>
    </step>

  </benchmark>
</jube>

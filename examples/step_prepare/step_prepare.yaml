name: step_prepare_example
outpath: bench_run
comment: "A step prepare example"

step:
  - name: prepare_step
    prepare: "execution_step"
    do: "python3 --version || exit 1"
  - name: execution_step
    tag: "execute"
    do: "python3 -c 'import math; print(math.factorial(10))'"
